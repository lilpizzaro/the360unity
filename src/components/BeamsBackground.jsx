'use client';

// Empty component that renders nothing, keeping only the original gradient background
export default function BeamsBackground({ 
  beamWidth = 2,
  beamHeight = 15,
  beamNumber = 12,
  lightColor = "#ffffff",
  speed = 2,
  scale = 0.2,
  rotation = 0,
  opacity = 0.5,
}) {
  // Return null to render nothing
  return null;
} 